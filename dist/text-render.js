import {textPose} from './creative.js';

const color=(value,fallback='#ffffff')=>/^#[0-9a-f]{6}$/i.test(value||'')?value:fallback;
const safeFont=value=>String(value||'Sans').replace(/["'`;{}]/g,'').slice(0,80)||'Sans';
const alphaColor=(hex,alpha)=>{const h=color(hex,'#000000');return`rgba(${parseInt(h.slice(1,3),16)},${parseInt(h.slice(3,5),16)},${parseInt(h.slice(5,7),16)},${Math.max(0,Math.min(1,alpha))})`};
export function textStyle(text={}){return{
 font:safeFont(text.font),italic:!!text.italic,align:['left','center','right'].includes(text.align)?text.align:'center',weight:[400,500,600,700,800].includes(+text.weight)?+text.weight:600,
 color:color(text.color),lineColors:(Array.isArray(text.lineColors)?text.lineColors:[]).slice(0,20).map(value=>color(value)),accentWords:String(text.accentWords??'').split(/[、,\n]+/).map(value=>value.trim()).filter(Boolean).slice(0,20),accentColor:color(text.accentColor,'#ff817b'),outline:Math.max(0,Math.min(20,+text.outline||0)),outlineColor:color(text.outlineColor,'#000000'),outerOutline:Math.max(0,Math.min(20,+text.outerOutline||0)),outerOutlineColor:color(text.outerOutlineColor,'#ffffff'),
 shadow:!!text.shadow,shadowColor:color(text.shadowColor,'#000000'),shadowOpacity:Math.max(0,Math.min(1,Number.isFinite(+text.shadowOpacity)?+text.shadowOpacity:.72)),shadowBlur:Math.max(0,Math.min(40,+text.shadowBlur||0)),shadowX:Math.max(-50,Math.min(50,Number.isFinite(+text.shadowX)?+text.shadowX:4)),shadowY:Math.max(-50,Math.min(50,Number.isFinite(+text.shadowY)?+text.shadowY:6)),
 letterSpacing:Math.max(-10,Math.min(50,+text.letterSpacing||0)),lineHeight:Math.max(.6,Math.min(3,+text.lineHeight||1.12)),box:['none','dark','light','custom'].includes(text.box)?text.box:'none',boxColor:color(text.boxColor,'#000000'),boxOpacity:Math.max(0,Math.min(1,Number.isFinite(+text.boxOpacity)?+text.boxOpacity:.62)),boxPadding:Math.max(0,Math.min(1,Number.isFinite(+text.boxPadding)?+text.boxPadding:.24)),boxRadius:Math.max(0,Math.min(.5,Number.isFinite(+text.boxRadius)?+text.boxRadius:.12))
}}
export const fontFamily=font=>font==='Serif'?'Georgia, "Times New Roman", serif':font==='Mono'?'"SFMono-Regular", Menlo, monospace':font==='Sans'?'Arial, "Hiragino Sans", sans-serif':`"${safeFont(font)}", Arial, sans-serif`;
const lines=text=>String(text.text??'').slice(0,2000).split('\n').slice(0,20);
const lineWidth=(ctx,line,spacing)=>ctx.measureText(line||' ').width+Math.max(0,[...line].length-1)*spacing;
function accentMask(line,words){const chars=[...line],mask=Array(chars.length).fill(false);for(const word of words){const target=[...word];if(!target.length)continue;for(let i=0;i<=chars.length-target.length;i++)if(target.every((char,j)=>chars[i+j]===char))for(let j=0;j<target.length;j++)mask[i+j]=true}return mask}
const lineColor=(style,index)=>style.lineColors[index]||style.color;
function drawSpaced(ctx,line,x,y,maxWidth,spacing,align,method='fillText',paint){const chars=[...line],width=Math.min(maxWidth,lineWidth(ctx,line,spacing));let at=align==='left'?x:align==='right'?x-width:x-width/2;ctx.textAlign='left';for(let i=0;i<chars.length;i++){if(paint)paint(i);ctx[method](chars[i],at,y,maxWidth);at+=ctx.measureText(chars[i]).width+spacing}}
function roundRect(ctx,x,y,w,h,r){r=Math.min(r,w/2,h/2);ctx.beginPath();ctx.moveTo(x+r,y);ctx.arcTo(x+w,y,x+w,y+h,r);ctx.arcTo(x+w,y+h,x,y+h,r);ctx.arcTo(x,y+h,x,y,r);ctx.arcTo(x,y,x+w,y,r);ctx.closePath()}
function boxFill(style){return style.box==='light'?'rgba(246,247,239,.82)':style.box==='dark'?'rgba(5,8,6,.62)':alphaColor(style.boxColor,style.boxOpacity)}
function drawRows(ctx,rows,{x,y,maxWidth,lineHeight,spacing,align,style,scale=1}){
 const masks=rows.map(line=>accentMask(line,style.accentWords)),fillRow=(line,index)=>drawSpaced(ctx,line,x,y+(index-(rows.length-1)/2)*lineHeight,maxWidth,spacing,align,'fillText',char=>ctx.fillStyle=masks[index][char]?style.accentColor:lineColor(style,index)),strokeRows=()=>rows.forEach((line,index)=>drawSpaced(ctx,line,x,y+(index-(rows.length-1)/2)*lineHeight,maxWidth,spacing,align,'strokeText'));
 if(style.shadow){ctx.shadowColor=alphaColor(style.shadowColor,style.shadowOpacity);ctx.shadowBlur=style.shadowBlur*scale;ctx.shadowOffsetX=style.shadowX*scale;ctx.shadowOffsetY=style.shadowY*scale}
 rows.forEach(fillRow);
 ctx.shadowColor='transparent';ctx.shadowBlur=ctx.shadowOffsetX=ctx.shadowOffsetY=0;
 if(style.outerOutline){ctx.strokeStyle=style.outerOutlineColor;ctx.lineWidth=(style.outline+style.outerOutline)*2*scale;strokeRows()}
 if(style.outline){ctx.strokeStyle=style.outlineColor;ctx.lineWidth=style.outline*2*scale;strokeRows()}
 rows.forEach(fillRow);
}
export function drawTextCanvas(ctx,text,time,w,h){
 const pose=textPose(text,time);if(!pose.alpha)return false;const style=textStyle(text),rows=lines(text),size=Math.max(1,Math.min((+text.size||48)*h/1080,h*.9/(rows.length*style.lineHeight))),lineHeight=size*style.lineHeight,unit=h/1080,spacing=style.letterSpacing*unit,maxWidth=w*.92;
 ctx.save();ctx.globalAlpha=pose.alpha;ctx.font=`${style.italic?'italic ':''}${style.weight} ${size}px ${fontFamily(style.font)}`;ctx.textAlign=style.align;ctx.textBaseline='middle';ctx.lineJoin='round';
 const x=pose.x*w,y=pose.y*h,width=Math.min(maxWidth,Math.max(...rows.map(line=>lineWidth(ctx,line,spacing)),1)),height=Math.max(lineHeight,rows.length*lineHeight),left=style.align==='left'?x:style.align==='right'?x-width:x-width/2;
 ctx.translate(x,y);ctx.scale(pose.scale||1,pose.scale||1);ctx.translate(-x,-y);
 if(style.box!=='none'){const pad=size*style.boxPadding;ctx.fillStyle=boxFill(style);roundRect(ctx,left-pad,y-height/2-pad,width+pad*2,height+pad*2,size*style.boxRadius);ctx.fill()}
 drawRows(ctx,rows,{x,y,maxWidth,lineHeight,spacing,align:style.align,style,scale:unit});ctx.restore();return true;
}
export function textRasterPlacement(text,raster,w,h,pose=textPose(text,(+text.start||0)+.001)){const rw=Math.max(1,+raster.width||1),rh=Math.max(1,+raster.height||1),refH=Math.max(1,+raster.referenceHeight||1080),scale=Math.min(h/refH,w*.92/rw,h*.9/rh),anchorX=Math.max(0,Math.min(rw,+raster.anchorX||rw/2)),anchorY=Math.max(0,Math.min(rh,+raster.anchorY||rh/2));return{rw,rh,scale,anchorX,anchorY,x:pose.x*w,y:pose.y*h,alpha:pose.alpha,poseScale:pose.scale||1}}
export function drawTextRasterCanvas(ctx,text,image,time,w,h){
 const pose=textPose(text,time);if(!pose.alpha||!image)return false;const layout=textRasterPlacement(text,text.raster||{width:image.width,height:image.height},w,h,pose),{rw,rh,scale,anchorX,anchorY,x,y}=layout;
 ctx.save();ctx.globalAlpha=pose.alpha;ctx.translate(x,y);ctx.scale((pose.scale||1)*scale,(pose.scale||1)*scale);ctx.drawImage(image,-anchorX,-anchorY,rw,rh);ctx.restore();return true;
}
export function rasterizeText(text){
 const style=textStyle(text),rows=lines(text),size=Math.max(8,Math.min(300,+text.size||48,1080*.84/(rows.length*style.lineHeight))),probe=document.createElement('canvas').getContext('2d');probe.font=`${style.italic?'italic ':''}${style.weight} ${size}px ${fontFamily(style.font)}`;
 const maxWidth=1920*.88,lineHeight=size*style.lineHeight,spacing=style.letterSpacing,basePad=size*(style.box==='none'?.08:style.boxPadding),effectPad=(style.outline+style.outerOutline)*2+(style.shadow?style.shadowBlur+Math.max(Math.abs(style.shadowX),Math.abs(style.shadowY)):0),pad=Math.ceil(basePad+effectPad),contentWidth=Math.min(maxWidth,Math.max(...rows.map(line=>lineWidth(probe,line,spacing)),1)),width=Math.max(2,Math.ceil(contentWidth+pad*2)),height=Math.max(2,Math.ceil(rows.length*lineHeight+pad*2));
 const canvas=document.createElement('canvas');canvas.width=width;canvas.height=height;const ctx=canvas.getContext('2d');ctx.font=`${style.italic?'italic ':''}${style.weight} ${size}px ${fontFamily(style.font)}`;ctx.textAlign='center';ctx.textBaseline='middle';ctx.lineJoin='round';
 if(style.box!=='none'){ctx.fillStyle=boxFill(style);roundRect(ctx,effectPad,effectPad,width-effectPad*2,height-effectPad*2,size*style.boxRadius);ctx.fill()}
 drawRows(ctx,rows,{x:width/2,y:height/2,maxWidth,lineHeight,spacing,align:'center',style,scale:1});const anchorX=style.align==='left'?pad:style.align==='right'?width-pad:width/2;return{data:canvas.toDataURL('image/png'),width,height,anchorX,anchorY:height/2,referenceWidth:1920,referenceHeight:1080};
}
export async function withTextRasters(project){const copy=structuredClone(project),visible=item=>!copy.overlayTracks?.[item.layer||0]?.hidden,texts=copy.texts.filter(visible);if(document.fonts){await Promise.all(texts.map(text=>document.fonts.load(`${textStyle(text).weight} 96px ${fontFamily(text.font)}`,text.text.slice(0,100)).catch(()=>[])));await document.fonts.ready}copy.texts=texts.map(text=>({...text,raster:rasterizeText(text)}));copy.effects=(copy.effects||[]).filter(visible);return copy}

export class TextPreview{
 constructor(root){this.root=root;this.nodes=new Map();this.signature='';this.canvas=document.createElement('canvas');this.canvas.className='text-preview-canvas';this.canvas.setAttribute('aria-hidden','true');root.prepend(this.canvas)}
 sync(texts,force=true){const collectionKey=texts.map(t=>t.id).join('|');if(!force&&this.collectionKey===collectionKey)return;this.collection=texts;this.collectionLength=texts.length;this.collectionKey=collectionKey;const signature=texts.map(t=>JSON.stringify([t.id,t.text,textStyle(t)])).join('|');if(signature===this.signature)return;this.signature=signature;const wanted=new Set(texts.map(t=>t.id));for(const[id,node]of this.nodes)if(!wanted.has(id)){node.remove();this.nodes.delete(id)}
  for(const text of texts){let node=this.nodes.get(text.id);if(!node){node=document.createElement('div');node.className='text-item';node.setAttribute('role','img');this.root.append(node);this.nodes.set(text.id,node)}node.dataset.textId=text.id;node.setAttribute('aria-label',String(text.text||''));const style=textStyle(text),fragment=document.createDocumentFragment();lines(text).forEach((line,index)=>{const row=document.createElement('span'),outer=document.createElement('span'),fill=document.createElement('span'),mask=accentMask(line,style.accentWords),chars=[...line];row.className='text-line';outer.className='text-outer';fill.className='text-fill';outer.setAttribute('aria-hidden','true');fill.setAttribute('aria-hidden','true');outer.textContent=line||'\u00a0';fill.style.color=lineColor(style,index);let start=0;for(let i=1;i<=chars.length;i++)if(i===chars.length||mask[i]!==mask[start]){const span=document.createElement('span');span.textContent=chars.slice(start,i).join('');if(mask[start])span.style.color=style.accentColor;fill.append(span);start=i}if(!chars.length)fill.append(document.createTextNode('\u00a0'));row.append(outer,fill);fragment.append(row)});node.replaceChildren(fragment);node.style.fontFamily=fontFamily(style.font);node.style.fontWeight=style.weight;node.style.fontStyle=style.italic?'italic':'normal';node.style.textAlign=style.align;node.style.color=style.color;node.style.webkitTextStroke='';node.style.textShadow='';node.style.paintOrder='stroke fill';node.style.lineHeight=style.lineHeight;node.style.background=style.box==='none'?'':boxFill(style);node.style.padding=style.box==='none'?'0':`${style.boxPadding}em`;node.style.borderRadius=style.box==='none'?'0':`${style.boxRadius}em`}
 }
 update(texts,time,scale,playing=false){this.sync(texts,!playing);const width=Math.max(1,this.root.clientWidth),height=Math.max(1,this.root.clientHeight),ratio=Math.min(2,globalThis.devicePixelRatio||1);if(this.canvas.width!==Math.round(width*ratio)||this.canvas.height!==Math.round(height*ratio)){this.canvas.width=Math.round(width*ratio);this.canvas.height=Math.round(height*ratio)}const ctx=this.canvas.getContext('2d');ctx.clearRect(0,0,this.canvas.width,this.canvas.height);for(const text of texts)drawTextCanvas(ctx,text,time,this.canvas.width,this.canvas.height);for(const text of texts){const node=this.nodes.get(text.id),pose=textPose(text,time),style=textStyle(text);node.hidden=!pose.alpha;if(!pose.alpha)continue;node.style.left=pose.x*100+'%';node.style.top=pose.y*100+'%';const layout=JSON.stringify([text.text,text.size,style,scale,this.root.clientWidth,this.root.clientHeight]);if(node.dataset.layout!==layout){node.dataset.layout=layout;node.style.fontSize=Math.max(1,(+text.size||48)*scale)+'px';node.style.letterSpacing=style.letterSpacing*scale+'px';const fit=Math.min(1,this.root.clientWidth*.92/Math.max(1,node.scrollWidth),this.root.clientHeight*.9/Math.max(1,node.scrollHeight));node.dataset.fit=String(fit)}const base=style.align==='left'?'translate(0,-50%)':style.align==='right'?'translate(-100%,-50%)':'translate(-50%,-50%)';node.style.opacity='0';node.style.background='transparent';node.style.transform=`${base} scale(${(node.dataset.fit||1)*(pose.scale||1)})`;node.style.transformOrigin=style.align==='left'?'left center':style.align==='right'?'right center':'center'}}
 clear(){for(const node of this.nodes.values())node.remove();this.nodes.clear();const ctx=this.canvas.getContext('2d');ctx.clearRect(0,0,this.canvas.width,this.canvas.height);this.signature='';this.collection=null;this.collectionLength=0;this.collectionKey=''}
}
