import assert from 'node:assert/strict';
import{project,clip,timing,sourceOffset,outputOffset,sequence,total,sanitize,speedToSliderPosition,sliderPositionToSpeed}from'../dist/model.js';
const m={id:'m',duration:12,width:1920,height:1080};
assert.equal(sliderPositionToSpeed(0),.05);assert.equal(sliderPositionToSpeed(1000),1);assert.equal(sliderPositionToSpeed(2000),20);let previous=.05;for(let i=1;i<=2000;i++){const speed=sliderPositionToSpeed(i);assert.ok(speed>=previous&&speed<=20);previous=speed}assert.equal(speedToSliderPosition(.05),0);assert.equal(speedToSliderPosition(1),1000);assert.equal(speedToSliderPosition(20),2000);assert.ok(Math.abs(sliderPositionToSpeed(speedToSliderPosition(1.2375))-1.2375)<.002);
for(const curve of ['constant','linear','ease-in','ease-out','ease-in-out']){let c={...clip(m),speed:.05,endSpeed:20,curve};const d=timing(c).duration;assert.ok(d>0);for(let i=0;i<=100;i++){let s=12*i/100;assert.ok(Math.abs(sourceOffset(outputOffset(s,c),c)-s)<1e-8)}if(curve==='constant')assert.equal(d,240)}
let p=project();p.media=[m];p.clips=[{...clip(m),out:2,speed:2},{...clip(m),in:2,out:3,speed:.1},{...clip(m),gap:2}];assert.equal(total(p),13);assert.deepEqual(sequence(p).map(r=>r.start),[0,1,11]);let restored=sanitize(JSON.parse(JSON.stringify(p)));assert.equal(restored.clips.length,3);assert.equal(restored.videoTracks.length,3);assert.equal(restored.overlayTracks.length,3);restored=sanitize({...JSON.parse(JSON.stringify(p)),videoTracks:[{hidden:true,volume:1.4}],overlayTracks:[{hidden:true}]});assert.equal(restored.videoTracks[0].hidden,true);assert.equal(restored.videoTracks[0].volume,1.4);assert.equal(restored.videoTracks[1].hidden,false);assert.equal(restored.overlayTracks[0].hidden,true);const overlayOnly=project();overlayOnly.effects=[{type:'black-out',start:2,duration:3}];overlayOnly.texts=[{text:'TITLE',start:1,end:8}];assert.equal(total(overlayOnly),8);assert.throws(()=>sanitize({version:2,clips:[],media:[]}));const precise={...clip(m),speed:1.2375,endSpeed:7.315};p=project();p.media=[m];p.clips=[precise];restored=sanitize(JSON.parse(JSON.stringify(p)));assert.equal(restored.clips[0].speed,1.2375);assert.equal(restored.clips[0].endSpeed,7.315);console.log('Timing inverse, arbitrary decimal speeds, ramp curves, gaps, overlay-only duration, track visibility and serialization: PASS');
