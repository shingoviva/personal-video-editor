import assert from 'node:assert/strict';
import {project,clip,sequence,sourceOffset,timing} from '../dist/model.js';
import {outputSettings,sourceTime,gainAt,mixWindow,limitStereo} from '../dist/mobile-model.js';
import {translation,motionEstimate,smoothPath,correctionAt} from '../dist/mobile-stabilize.js';
import {safeName} from '../dist/device-save.js';
const p=project(),m={id:'source',width:3840,height:2160,fps:240,duration:12};p.media=[m];p.clips=[clip(m)];
p.aspect='9:16';assert.deepEqual([outputSettings(p).width,outputSettings(p).height],[1080,1920]);p.export.fps='Source';assert.equal(outputSettings(p).fps,60);
p.export.resolution='1440p';assert.deepEqual([outputSettings(p).width,outputSettings(p).height],[1440,2560]);
p.export.resolution='4K';assert.deepEqual([outputSettings(p).width,outputSettings(p).height],[2160,3840]);
p.export.preset='YOUTUBE';p.export.quality='High';assert.equal(outputSettings(p).audioBitrate,320000);
p.export.videoBitrate='35';p.export.audioBitrate='256';assert.equal(outputSettings(p).bitrate,35000000);assert.equal(outputSettings(p).audioBitrate,256000);
const overlayOnly=project();overlayOnly.texts=[{id:'title',start:2,end:5,text:'TITLE'}];overlayOnly.effects=[{id:'fade',type:'black-out',start:5,duration:.6}];const overlayOutput=outputSettings(overlayOnly);assert.equal(overlayOutput.duration,5.6);assert.equal(overlayOutput.frames,Math.ceil(5.6*overlayOutput.fps));assert.equal(overlayOnly.clips.length,0);
for(const curve of ['constant','linear','ease-in','ease-out','ease-in-out'])for(const speed of [.05,.1,1,20]){const c=p.clips[0];Object.assign(c,{in:2,out:7,speed,endSpeed:.2,curve,hold:.8});const row=sequence(p)[0],end=timing(c).duration;for(let t=0;t<end;t+=end/51){assert.ok(Math.abs(sourceTime(row,t)-Math.min(c.out-1e-6,c.in+sourceOffset(t,c)))<1e-8)}}
assert.equal(gainAt(0,4,1,1,1),0);assert.equal(gainAt(2,4,.7,1,1),.7);assert.equal(gainAt(4,4,1,1,1),0);
const data=new Float32Array(8);mixWindow(data,0,4,[new Float32Array([0,1,0,-1,0]),new Float32Array([1,0,-1,0,1])],i=>i*.5,()=>.5);assert.deepEqual([...data],[0,.25,.5,.25,.5,.25,0,-.25]);
const aliased=new Float32Array(40),nyquist=Float32Array.from({length:64},(_,i)=>i%2?1:-1);mixWindow(aliased,0,20,[nyquist,nyquist],i=>8+i*2,()=>1);assert.ok(Math.max(...aliased.map(Math.abs))<.2);const hot=new Float32Array([2,.5,.25,0,2,.5,.25,0]);const state=limitStereo(hot);assert.ok(Math.max(...hot.map(Math.abs))<=.950001);assert.ok(state.gain<1);
let seed=101;const n=48,a=Float32Array.from({length:n*n},()=>{seed=(seed*1664525+1013904223)>>>0;return seed%255}),b=new Float32Array(n*n);for(let y=0;y<n-1;y++)for(let x=0;x<n-2;x++)b[(y+1)*n+x+2]=a[y*n+x];assert.deepEqual(translation(a,b),[2/n,1/n]);assert.deepEqual(translation(new Float32Array(n*n),new Float32Array(n*n)),[0,0]);
assert.ok(motionEstimate(a,b).confidence>0);const rn=72,ra=Float32Array.from({length:rn*rn},()=>{seed=(seed*1664525+1013904223)>>>0;return seed%255}),rb=new Float32Array(rn*rn),rotation=.035,center=(rn-1)/2,cos=Math.cos(-rotation),sin=Math.sin(-rotation);for(let y=0;y<rn;y++)for(let x=0;x<rn;x++){const dx=x-center,dy=y-center,sx=Math.round(center+cos*dx-sin*dy),sy=Math.round(center+sin*dx+cos*dy);if(sx>=0&&sx<rn&&sy>=0&&sy<rn)rb[y*rn+x]=ra[sy*rn+sx]}const rotated=motionEstimate(ra,rb,rn);assert.ok(rotated.confidence>.5&&Math.abs(rotated.angle-rotation)<.01);const moving=Array.from({length:30},(_,i)=>({time:i/12,x:i*.02+(i%2?.015:0),y:0,angle:i*.001+(i%2?.004:0)})),path=smoothPath(moving,'HORIZON');assert.ok(path.scale>1&&path.scale<=1.3);assert.ok(Math.abs(correctionAt(path,1).x)<=.1);assert.ok(Number.isFinite(correctionAt(path,1).angle));const before=moving.map((p,i)=>p.x-i*.02),after=moving.map((p,i)=>p.x+correctionAt(path,p.time).x-i*.02),energy=a=>a.reduce((s,v)=>s+v*v,0);assert.ok(energy(after)<energy(before)*.7);assert.equal(safeName('a/b:test','mp4'),'a_b_test.mp4');
console.log('Mobile: output sizes/FPS, speed/holds, audio, translation/rotation stabilization, crop and filenames PASS');
