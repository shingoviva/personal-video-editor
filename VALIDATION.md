# Validation — 2026-09-08

検証環境：Linux x86_64、FFmpeg 6.1系、Python 3.12、Node.js。合成映像に加え、ユーザー提供のH.264/SDR実写素材を使用しました。クラウドChromeで一部UI操作も確認しました。Apple Siliconでの速度測定・実写HDRの色評価・SNS実投稿は未実施です。

## 処理パイプライン

| 試験 | 結果 |
|---|---|
| 4K・60fps・10bit HEVC/HLG生成 → SDRプロキシ | PASS |
| HLG HEVC 4K → 1080p60 H.264 Rec.709 | PASS |
| VFRの検出 → CFR出力 | PASS |
| 120fps音声なし → H.264/AAC | PASS |
| 240fps入力 → 60fps出力 | PASS |
| Trim / 9:16 Crop / 音量・Fade | PASS |
| 0.05倍・20倍 | PASS |
| Ease In/Out 0.1→8倍 + 原音同期 | PASS |
| Frame blending + color + text + hold | PASS |
| vidstab検出・補正2パス | PASS |
| 複数クリップ + 黒の空白 + BGM | PASS |
| Best Moment・Intent別の特徴量解析 | PASS |
| キャンセル | PASS |
| 元素材ハッシュ不変 | PASS |

出力はffprobeでコーデック・映像寸法・映像と音声の長さを確認しています。AACパケットとフレーム境界の丸めによる差を認め、映像/音声の長さ差と期待尺の差は150ms以内を合格条件にしました。ケース別の実測値は `validation/pipeline.json` を参照してください。

解析の全カテゴリはアルゴリズムを実装していますが、実写の検出品質を評価した結果ではありません。表情・ポーズ・視線の意味認識や専門家による候補の採点は含みません。

## 状態・API

- JavaScriptの構文検証：PASS。
- 時間マッピングの相互変換、0.05〜20倍、4種のランプ、空白、プロジェクトJSON：PASS。
- ローカルAPI：未認証拒否、別Origin拒否、メディアのRange配信、ディスク保存/復元、ストリーム読み込み、非同期プロキシジョブ：PASS。
- クラウドChrome：合成素材のImport、WebGL画面表示、再生/停止、フレーム送り、分割、0.05倍→1倍Ease In/Out設定、9:16、COLDルック、Undo/Redo、自動保存後の復帰、再リンク、Best Momentスキャン候補表示を確認。
- `.project` のダウンロード完了イベントは取得できず、実機でのファイル保存・再読込は未検証。UIからの開始と自動保存は動作。
- Safari、ブラウザHEVCデコード、音声試聴、滑らかさの定量評価は未実施。

## メモリ

4K60 HEVC/HLG → 720p30 SDRプロキシ相当のFFmpeg処理、入力とフィルタ・エンコーダのスレッド数を2に制限して測定。

| 入力尺 | 子プロセスの最大RSS |
|---|---:|
| 0.6秒 | 377.5 MiB |
| 6秒 | 379.6 MiB |

短尺で、動画の長さに比例したフレーム蓄積が生じないことを確認しました。長時間稼働・全機能・実機でのリークがないことを保証する測定ではありません。詳細は `validation/memory.json`。

## 採用構成と理由

- HTML/CSS/ES modules：依存パッケージなしで配布し、同じ画面をブラウザとMacで使用。
- HTMLVideoElement＋WebGL：低負荷の近似試写。4KはMacでプロキシ化。
- ネイティブFFmpeg：HEVC、HDR、VFR、vidstab、H.264/AACを同じパイプラインで処理。
- Web Worker：ブラウザのフレーム特徴量計算。毎フレームの画素バッファは処理後に解放。
- Python標準ライブラリ：ローカルHTTPとジョブ制御。127.0.0.1にのみバインドし、起動トークンで認証。
- CPU libx264：この環境で検証できるエンコーダを採用。VideoToolboxは能力の検出のみ。
- FFmpeg.wasm：大きな素材の複製・メモリ圧迫・スタビライズ構成の制約を避けるため、この版では不採用。
- WebCodecs：コーデック対応がブラウザ実装に依存し、HEVC/HDRを必須とするV1の主エンジンには不採用。

## V1受け入れ前に必要な実素材試験

1. iPhoneのSDR 4K60、Dolby Vision/HLG、1080p120/240、縦動画、長尺VFRを各1本。
2. ShingoさんのApple Silicon Macで起動し、ブラウザの動画再生とプロキシ生成を確認。
3. 肌・白い衣服・照明ハイライトを含む映像で、HDR→SDRとカラーの色を評価。
4. 激しいパン・歩き撮り・髪や服の動きで、スタビライズと候補検出を評価。
5. ランプ境界の音、数十分の連続編集、キャンセルと再試行、終了後の再開を確認。
6. Instagramへ実投稿し、画角・再生・色・音声を確認。

未検証項目を合格扱いにはしていません。詳しい操作上の制約は README.md を参照してください。

## 1.1.0 信頼性改善

- MP4/WAVは一時ファイルへ生成し、映像/音声の有無・長さ・コンテナの検証後に確定。
- 不完全な生成物に限り1回再生成。元ファイルや設定のエラーは再試行しない。
- 最終出力はFFmpegで全尺のデコード検証を実施。
- 診断ログを保存し、認証済みAPIからのみ取得可能。
- ブラウザの素材/BGM読み込みと解析シークにタイムアウト・キャンセル・後処理を追加。
- 短いタイムラインは小数秒の目盛りで表示。再リンク待ちに以前の映像を残さない。

`tests/reliability.test.py` の6試験（1回復旧、上限2回と完成済みファイル保護、入力エラー非再試行、キャンセル、音声先頭ストリームの全尺検証、破損プロキシ再生成）全件PASS。`tests/media-state.test.mjs` は成功・即時イベント・キャンセル・タイムアウト・読み込みエラーを検証。更新パイプラインと認証付き診断APIもPASS。

## 実写素材

入力：512×910、H.264 High、8bit SDR Rec.709、30fps CFR、AAC、18.633秒。4K/HEVC/HDR素材ではありません。

- 全尺の1080×1920 H.264/AAC出力、0.05倍・20倍・ランプ混在出力を検証。アップスケールは元映像の細部を増やしません。
- 4〜10秒区間をNATURAL補正、1080×1920で書き出し。1.1.0で2回成功、全尺デコード検証済み。33.16秒/41.65秒（Linuxでの参考値、Mac性能を表しません）。
- 初回の旧処理で中間MP4の `moov atom not found` を1回観測。原因は未特定。同条件の旧処理再試行2回と更新版2回は成功。自動再生成が元の原因を解消したとは結論しません。
- 元素材のハッシュ不変を確認。人物・髪の動きにBest/Motion候補あり。この素材では静止/低変化区間の候補なし。人物意味認識の品質試験ではありません。
- `validation/reliability-real.json` は集計のみ。実写動画本体は同梱していません。

## 1.2.0 iPhone / 端末内書き出し

追加経路：Mediabunny 1.56.0 + WebCodecs + Worker + OffscreenCanvas。H.264/AACを時刻指定で生成し、OPFSへ位置指定の小さな書き込みを行います。Fast Startはメタデータ領域の事前予約方式。全フレームと音声をメモリへ蓄積しません。処理後に映像・音声を全尺デコードし、フレーム数・コーデック・尺を検証します。

端末内に素材のBlobをIndexedDBで保存し、自動保存されたプロジェクトから再開します。元の写真ライブラリは変更しません。完成MP4は共有画面またはdownloadリンクで保存し、直近の完成出力をOPFSに保持します。共有操作が返ったことを写真への保存完了とは判定しません。

### 実施した試験

- 出力寸法（1080p/4K、9:16）、Source FPS上限60：PASS。
- 全4ランプ、0.05〜20倍、Holdを含む映像時刻計算：既存モデルとの一致PASS。
- ステレオ補間、音量、Fade、補正用の既知の平行移動推定とcrop上限：PASS。
- 本番と同じMediabunny MP4 muxer / StreamTargetでH.264/AACの実パケットを位置指定書き込み。先頭音声ストリームの合成入力、Fast Start、ffprobe尺検証とFFmpeg全尺デコード：PASS。
- 実PCM音声をストリームで読み、サンプル境界以外からの区間取得、速度ランプ混合、Hold/Mute、BGMループ境界を検証：PASS。
- JavaScript構文・静的アセット参照、従来の時間モデル・メディアイベント試験：PASS。
- アプリの「端末版の対応状況」に短尺の書き出しテストを実装。ユーザー端末でWorker・デコード・GPU・エンコード・OPFS・出力検証を実際に通せます。テストは編集プロジェクトを変更しません。

### 未検証・非同等の項目

この実装はiPhone実機で未検証です。上記のMP4試験はmuxer/IOの検証であり、iOSのWebCodecsエンコーダ・GPU処理を通した試験ではありません。iPhone Safariでのタッチ操作・写真共有保存・ファイル保存・長尺/4Kメモリ測定は未実施です。「完全対応済み」とはしません。

- HDRは検出して停止します。ブラウザの不確かな暗黙変換で色を確定しないためです。現状、正確なHLG/PQ→SDRはMac版が必要です。
- 端末内の原音速度変更はリサンプリングで、音程も変化します。BGMは通常速度。Mac版のピッチ維持とは異なります。
- 端末内スタビライズは低解像度での平行移動追跡と平滑化です。回転・パース・ローリングシャッターやvidstab同等の補正は行いません。品質は実写で未評価です。
- Frame blendingは隣接フレームの時刻補間です。光学フローではありません。
- 画面が背景に移ると書き出しを中止し、編集を保持します。バックグラウンド書き出しは保証しません。
- 端末版はWebCodecsのH.264/AAC、Worker内OffscreenCanvas/WebGL、OPFS同期アクセスに依存します。バージョン番号だけで対応済みとはせず、実際の能力を判定します。
- ブラウザ内の保存は、OSの空き容量不足やサイトデータの削除で失われる場合があります。写真/ファイルへ完成品を保存し、元素材は保持する必要があります。

参照： https://mediabunny.dev/guide/writing-media-files 、 https://mediabunny.dev/guide/media-sinks 、 https://webkit.org/blog/13862/the-user-activation-api/ 。バンドルにMITライセンスを同梱。

## 1.3.0 — 2レイヤー / 直接編集 / プレビュー負荷軽減

2026-09-09、Linux環境で確認。

- 2レイヤーの可視区間、上段優先、下段への復帰、空白、コピーの独立性、削除、同一レイヤーの前詰め、旧データの配置変換、JSON再読込を確認。
- Pointerイベントのユニットテストで左右トリム、1操作1チェックポイント、上下レイヤー移動、キャンセル時のハンドラ解除を確認。実ブラウザ／実機タッチ操作の検証ではない。
- Linear / Ease In / Ease Out / Ease In-Outのランプ分割で、元の時間積分とソース位置が保存されることを確認。
- ネイティブFFmpegで赤→青→赤の2レイヤーMP4を実生成。フレーム境界外の編集点、H.264/AAC、全尺デコード、RGBサンプル、原本SHA不変を確認。
- 端末版音声処理で、ミュートされた上段が下段原音を覆い、下段へ戻ると正しい時間の原音が復帰することを確認。端末エンコーダそのものは未検証。
- 既存のプロキシ、0.05x、20x、速度ランプ、カラー、テキスト、保持、手ぶれ補正、複数クリップとBGM、解析、120fpsのパイプライン試験が通過。
- 再生開始時に可視タイムラインをコンパイル。200クリップのテストで時刻検索100,000回が約5〜6ms（Nodeの参考値。iPhoneの再生FPSや動画デコード速度ではない）。
- 速度積分・GPU uniformをキャッシュ。再生中の描画は最大30回/秒、ドラッグ中は既存クリップDOMの位置と幅を更新。

未検証：iPhone/Safari実機、実機共有保存、長時間4Kの速度とメモリ、実ブラウザのジェスチャー。端末HDR非対応など1.2.0の制限は継続。今回、透過合成と2本の原音の同時ミックスは追加していない。

## 1.4.0 — 静止画・音声・素材管理

2026-09-09、Linux環境。

- 資産モデル試験：拡張子／MIMEによる種類判定、検索、種類フィルター、元ファイル名を保つ表示名変更、使用中素材の保護、未使用の除外、JSON復元を確認。
- 静止画の初期5秒、12秒への延長、画像プレビューの古い非同期デコードを破棄・解放する処理を確認。
- 実FFmpeg試験：PNG静止画を6秒へ延長＋H.264動画0.5秒＋WAVのBGMループをH.264/AAC MP4として書き出し。RGBサンプル、6.5秒の長さ、全尺デコード、元素材SHA不変を確認。
- 既存JSテスト一式（レイヤー・ジェスチャー・速度・端末コンテナ・端末音声）およびローカルサーバーの認証・アップロード・プロキシ・保存試験が通過。
- 音声付きファイルに埋め込まれたアルバム画像を、映像トラックと誤認しないようネイティブ解析を修正。
- 実ブラウザQA、iPhone実機、端末エンコーダでの静止画書き出しは未検証。画像デコードの破棄はモックを用いたユニット試験であり、実機性能を表すものではない。


## 1.5.0 — Creative tools

- tests/creative.test.mjs：静止の元時刻不変、尺キャッシュ更新、ランプと分割済みランプの伸縮、速度上限下限、マウス／2本指の1トランザクションとキャンセル、透明度／FX／文字の時刻を検証。
- 8ルック×4強度×3色（肌色・明部・暗部）の96組でJavaScriptとPythonのRGB計算が一致。MONOの強度150%でも無彩色を維持。
- tests/creative.test.py：赤V1／青V2を実際にH.264/AAC化。開始は赤、中間は両色、中央は青になる透明フェードを画素で検証。白フラッシュ・黒から映像・映像から黒も画素を検証。
- 動く合成素材の末尾0.999秒を2秒静止。元フレームをffprobeの実タイムスタンプから選び、離れた時刻のフレームが一致。静止＋Rise文字のMP4全尺デコードも通過。
- LUT出力の画素テストでRGB→YUV変換行列とBT.709タグの不一致を発見し修正。補正式との差を圧縮／量子化を含む許容範囲内で検証。
- 既存のJSモデル／音声／コンテナ試験、ネイティブの速度極値・ランプ・カラー・保持・文字・スタビライズ・BGM・解析・120fpsパイプラインを検証。
- ブラウザ／iPhone16 Pro／M2 MacBook Proで今回のUI・GPU・タッチ・実時間FPSは未検証。端末内合成コードの実エンコードは端末テストで確認が必要。LUT補間、コーデック、フォント、HDR/SDR色管理で完全な画素一致は保証しない。
