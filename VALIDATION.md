# Validation — 2026-09-23

## 2.1.0 — 任意速度・動き補正実験

速度と速度ランプの任意小数入力（0.05〜20×）を追加し、1.2375×の値がモデル保存とMac FFmpeg書き出しで保たれる回帰テストを追加しました。MaximumスローとAGGRESSIVE手ぶれ補正も公開版に含みます。`npm test`、静的ビルド、Python動画処理一式、ローカルHTTP配信、Mac ZIP生成を確認しています。実写比較はこれからです。AIモデルによる補間、M2の画質・速度、実写手ぶれ補正は未検証です。

## 2.0.8 — スロー再生・手ぶれ補正の強化

- `npm test` と静的ビルド検査を通過
- `tests/validate_pipeline.py`：UMH動き補償を使うスロー書き出し、尺、全フレームデコードを確認
- 解析フレーム数、UMH設定、vid.stab解析間隔の回帰確認を通過
- Mac FFmpegにvid.stabがないため、手ぶれ補正の実写比較は未実施。GitHub Actionsで動画処理全体を検証

## 2.0.7 — 動画末尾の外へのテキスト／FX配置と動画なし書き出し

- JavaScriptテスト一式（同梱Node.js）通過。動画なしプロジェクトの出力尺・フレーム数の回帰テストを追加
- 静的ビルド検査通過
- `tests/overlay-only.test.py`：黒背景、ラスターテロップ、FX、H.264/AAC、全尺デコードを確認
- iPhone Safari実機での受け入れ検証は未実施

## 2.0.6 — 可変ポイント・滑らかトーンカーブ

- Mac Chromeでポイントの追加、上下左右移動、右クリック削除、削除後の原状復帰を実操作確認
- 曲線操作前後の映像プレビュー画像を比較し、GPU反映を確認
- JavaScriptとMac FFmpegで任意位置ポイントの補間結果が一致することを確認
- 旧5点カーブの読み込み、最大16点制限、保存復元、カーブLUTを自動試験
- iPhone 16 Pro Safariと実写HDR素材の受け入れ試験は未実施

## 2.0.5 — iPhone相当カラー調整とトーンカーブ

- 全JavaScriptテスト、静的ビルド検査を通過
- Mac Chromeで20個のカラー項目表示、GPU初期化、トーンカーブの実ドラッグ更新を確認
- ブラウザとMac FFmpegのRGB変換を同じ入力値で比較し一致を確認
- 新しいカラー、カーブ、精細度、ノイズ除去を含むH.264/AAC MP4をFFmpegで生成し、全フレームデコードを確認
- Python動画処理テスト（H.264、ProRes、レイヤー、音声、テロップ、FX、静止画、YouTube）を通過
- iPhone 16 Pro Safariと実写HDR素材の受け入れ試験は未実施

## 2.0.4 — 詳細書き出し設定

- Mac ChromeでYouTube 4K・59.94fps・映像60Mbps・音声320kbps・任意ファイル名の連動を確認
- 3840×2160、7,193フレーム、推定容量約905MBの事前表示を確認
- ブラウザの明示ビットレート計算とMac FFmpegのH.264/AAC引数を検証
- JavaScript全テスト、YouTube、ProRes、ネイティブ動画・多重音声テスト通過

## 2.0.3 — AUDIO素材削除とヘルプ

- Mac ChromeでAUDIO LIBRARYの音声素材削除、確認画面、参照クリップ削除を確認
- 上部ヘルプを開き、5分野の操作ガイドと18ショートカットの表示を確認
- JavaScript全テスト、静的ビルド、音声多重化・YouTube・ネイティブ動画処理テスト通過

## 2.0.2 — タイムライン時間表示とシーク操作

- Mac Chrome 1600×1000でタイムラインを縦スクロール後も時間目盛りの上端固定を確認
- 再生ヘッドをドラッグし、0秒から76.80秒へ移動してプレビュー時刻との同期を確認
- 117.60秒の終端付近で時刻ラベルが表示領域内に収まることを確認
- JavaScript全テスト、静的ビルド、Python動画処理・Macエンジン・YouTubeテスト通過

## 2.0.1 — 手ぶれ補正・フォント・再リンク・テロップ一致

- 端末補正の複数領域動き推定、平行移動と回転の平滑化、HORIZON、自動クロップ率をユニット試験した。Mac側はHORIZONの2パス`vidstab`設定に高精度検出、42フレーム平滑化、ガウス最適化、bicubic補間が含まれることを確認した。
- Webフォント5書体、利用可能なシステムフォント、Local Font Access、OTF／TTF／WOFF／WOFF2読み込みの選択経路を追加した。読み込みフォントのメタデータは`.project`保存・再読込で保持され、実データはIndexedDBから復元する。
- 素材の再リンク候補を名前、容量、種類、尺で採点し、一致度順へ並べる試験を追加した。候補選択後も保存済み素材IDを保持する。
- テロップのプレビューと両書き出し経路を共通Canvas描画へ統一した。1080pと4Kでラスター倍率、アンカー位置、装飾寸法が正比例することを自動試験した。
- macOS Chromeの1600×1000画面で、Webフォント候補、システムTTFの読み込み、更新後のIndexedDB復元、装飾テロップのCanvas描画、再リンク候補の一致度順表示を操作した。1080pと4Kの実Canvas描画境界はほぼ正確に2倍となり、JavaScript例外は0件だった。既存翻訳がNotoの先頭を誤変換する問題も検出して修正した。
- JavaScript全試験、静的ビルド検査、Pythonのcapabilities/images/layers/multitrack/creative/youtube/program-workflow/overlay-only/prores/reliability/server/validate_formats/validate_pipelineを通過した。YouTube 4K寸法、ProRes 10-bit 4:2:2、VFR、240fps、複数映像・4音声、複数テロップの全尺デコードを含む。

実機Safari、iPhone 16 Pro、実写HDR、実写の回転揺れ・ローリングシャッター、長時間連続編集、第三者環境のフォント差は未検証です。

## 2.0.0 — UI再編・テロップ独立装飾

- ナビゲーションを`MEDIA / VIDEO / GRAPHICS / AUDIO / ASSIST`の5領域へ再編し、動画選択では`BASIC / MOTION / COLOR / LOOKS`を右設定欄内から移動できることを確認した。別の動画を選び直しても現在の動画サブメニューを保持し、TEXTから動画を選んだ場合はBASICへ移る。
- 1600×1000のmacOS Chromeで動画、テロップ、FX、音声の選択に応じた右設定欄と左上の選択表示を操作した。390×844を含めて横方向のページはみ出しはなく、JavaScript例外は0件だった。
- 2個のテロップへShift複数選択で同じ装飾を適用し、未選択テロップが変化しないことを確認した。別の2個へ黄と青の異なる装飾を順番に設定し、同時表示時に両方の色と輪郭が残ることをDOMと画面で確認した。
- テキスト属性ペーストでは本文、開始・終了、F/Tトラックを保持し、色、二重輪郭、影、背景、文字組み、位置、透明度、アニメーションだけが深いコピーで移る。自動保存後の再読込でも3テロップの状態が完全一致した。
- `npm test`、`npm run build`、Pythonのcapabilities/images/layers/multitrack/creative/youtube/program-workflow/overlay-only/prores/reliability/server/validate_formats/validate_pipelineを通過した。Mac ZIP（459,777 bytes）を生成して内容検査も通過した。

実機Safari、iPhone 16 Pro、実写HDR、長時間連続編集、第三者のMac環境は今回未検証です。

## 1.17.0 — 調整・透明度キーフレーム・オーバーレイ移動

- FX／TEXTの縦方向だけのドラッグを含むF/T1〜F/T3移動、横位置と尺の維持、終了時のレイヤー確定を自動試験した。
- 透明度キーフレームの滑らかな補間、プロジェクト保存・再読込、属性コピーの独立性を自動試験した。Mac FFmpegで赤い下段と青い上段を合成し、透明→不透明→透明の3時点を実画素で確認した。
- ガンマと黒浮かせを含むブラウザ／PythonのRGB計算一致、Mac側のLUT、シャープ、周辺光量フィルタ構成を検証した。
- 標準テロッププリセット、イタリックのプレビュー／ラスター／保存、プリセット適用後の文字組み維持を確認した。
- macOS Chrome 152・1600×1000で、動画選択時のADJUST切替、透明度点の保存、FXのF/T1→F/T3縦ドラッグ、標準テロップ4種、右クリックメニューの外側クリック解除を操作した。JavaScript例外は0件だった。
- `npm test`、`npm run build`、Pythonの全13動画処理試験、Mac ZIP生成を通過した。

iPhone 16 Pro Safari、Mac Safari、実写HDR、長時間連続編集、色調整のリファレンスモニターによる主観評価は今回未検証です。

## 1.16.0 — 英語表示・3テロップトラック・複数原音

- macOS Chrome 152で日本語／英語切替、MEDIA動画の無音ホバープレビュー、3映像・3FX・3テロップ・3音声項目を含む複雑な状態を作成し、自動保存後の更新と`.project`再読込が完全一致することを確認した。
- 1600×1000のChromeで左右の縦境界を220pxから290pxへドラッグし、FX／TEXT 3トラック、プレビュー上のテロップ本文・サイズ変更、テロップの別トラックへのドラッグ、右クリック、動画なしタイムラインの再生を確認した。JavaScript例外は0件だった。
- ブラウザ音声ミキサーとMac FFmpegで、縦に重なった3動画レイヤーの原音が同時に含まれ、各Vトラック音量が反映されることを周波数解析で確認した。AトラックのSolo時は従来どおり指定音声だけを出力する。
- Mac FFmpegで動画素材なしの黒背景＋テロップ＋FXを1.5秒のH.264/AACへ書き出し、全尺デコードと文字画素を確認した。
- `npm test`、`npm run build`、Pythonの全12既存動画処理試験、追加したoverlay-only試験、ローカルサーバー試験を通過した。

iPhone 16 Pro Safari、Mac Safari、実写HDR、長時間連続編集、第三者環境の受け入れ試験は今回未検証です。

## 1.15.2 — コピー／複製の上限処理

- 複数種類の選択を貼り付ける際、上限済みのFXを除外し、空きのあるテキストだけを元の相対時刻で追加できることを自動試験で確認した。
- テキスト120個の上限到達後は追加結果を空として返し、存在しないIDを選択対象にしないことを確認した。
- 映像と分離音声を複製した場合、新しい映像IDと音声IDが相互に正しく連動することを確認した。
- Macエンジンへ接続したChromeで1440×1000と390×844を確認し、テロップ追加・番組インパクト適用・横方向のはみ出しなしを確認した。未指定faviconの404も解消した。
- `npm test`、`npm run build`、Pythonのcapabilities/images/layers/multitrack/prores/reliability/server/pipeline/formats/creative/youtube/program-workflow、Mac ZIP生成を通過した。

iPhone 16 Pro Safari、実写HDR、長時間連続編集、第三者のMac Safariは今回未検証です。

## 1.15.1 — TV番組テロップ

- macOS Chrome 152で「番組二段・青赤」と「番組インパクト」を実際のタイムラインへ配置し、二重アウトライン、2行の白／赤切り替え、指定語のみ黒またはサーモン色へ変わることを目視確認した。
- 同じ編集をブラウザ経路で640×360・30fps・H.264/AACへ書き出し、180/180フレーム、映像尺6.000秒、全尺再デコードを確認した。1.5秒と4.2秒のプレビュー／完成フレームを比較し、文字、行色、強調語、内外フチの一致を確認した。
- 外フチが隣接する強調語の前面へ重なる問題を実操作中に検出し、プレビューを外フチ層と文字層へ分けて修正した。修正後の実Chrome操作ではJavaScript例外0件だった。

参考画像そのものの書体は利用環境のフォントに依存するため完全一致ではありません。第三者のMac SafariとiPhone Safariでの表示差は未検証です。

## 1.15.0 — 番組フロー・書き出し一致・可変ワークスペース

- 合成H.264/AAC素材をmacOS Chrome 152へ読み込み、黒タイトル、映像フェードイン、3つの連続テロップ、終端の暗転を画面操作で作成し、ブラウザ経路でMP4を書き出した。FX 2個、テキスト4個、境界ドラッグ78px、進捗画面の表示を確認し、操作中のJavaScript例外は0件だった。
- 出力映像は640×360・30fps・H.264 High・180フレーム・映像尺6.000秒。全尺再デコードを通過し、0.5／2.9／3.9／5.9秒のプレビューと出力画像を目視比較した。黒タイトル、黄・青テロップの切り替え、終端の完全な黒を確認した。
- AACの符号化パディングを含むトラック尺は6.080秒だった。編集映像は180フレームで6.000秒に固定し、音声は編集尺より短くないこととパディングが120ms以内であることを完成検証へ追加した。
- Mac FFmpeg経路でも実動画を生成し、黒保持、フェード中の画素、テロップの色・位置・サイズ、切り替え境界、終端暗転、H.264/AAC、全尺デコードを自動試験した。テロップの左／右揃えで生じていたラスター余白分の位置差も補正した。
- Black FadeのHoldと切り替え時間はプレビューと両書き出し経路で同じ半開区間に統一し、境界フレームで前後のテロップが重ならないことを確認した。

第三者のMac環境、Mac Safari、iPhone 16 Pro Safari、長尺YouTube素材の連続編集、主観画質・音質は未検証です。

## 1.14.0 — YouTube書き出し・テロップ・メモリ管理

- 16:9の1080p／1440p／4K出力寸法と、YouTube High／MaximumのAAC 320kbps設定を自動試験へ追加。
- テロップ120個の保存上限、装飾値の検査、標準字幕／肩書きプリセットを自動試験で確認。
- Mac側は複数テロップを単一のfilter graphで合成し、映像の再圧縮を一度に限定。
- プレビュー更新頻度を24／30／60fpsから選べること、GPUテクスチャとデコーダを解放できることを確認。
- macOS Chrome 152で実写 `IMG_7697.MP4` を読み込み、3種類のテロップを時間軸上の別区間へ配置して1080pを書き出した。出力は1920×1080・30fps・H.264 High・AACステレオ約308kbpsで、90/90フレームのハッシュがすべて異なり、全尺再デコードを通過した。冒頭・中盤・終盤を画像でも確認し、映像の動き、アウトライン、影、帯背景を目視確認した。
- 同じブラウザ経路で3840×2160・30fpsの4K H.264/AACも実生成し、30/30フレームがすべて異なること、全尺再デコード、テロップ描画を確認した。短尺の機能確認であり、長時間4K編集の速度・メモリ評価ではない。
- Macの表示値では搭載メモリ32GB、検証時のJavaScriptヒープ約8.2MB／上限約4.4GBを確認。手動解放、10秒／30秒後の自動解放設定を画面から操作した。長時間のリーク評価ではない。
- 実機Safariと第三者のMac環境での長時間受け入れ試験は未実施。

## 1.13.2 — 端末書き出しの動画フレーム修正

- 書き出し用の可変Canvasを毎フレームGPUへ再転送することを単体試験する。
- iPhone 16 ProのHEVC/SDR/VFR実写 `IMG_7697.MP4` から、ブラウザ経路で3秒と別区間2秒の1080×1920・30fps H.264/AAC MP4を2回書き出した。
- 1回目は90/90フレーム・90種類、2回目は60/60フレーム・60種類のフレームハッシュを確認した。両方を再デコードし、各区間の冒頭・中盤・終盤を目視して、被写体と画角が連続して変化していることを確認した。
- iPhone 16 Pro Safari実機での最終受け入れ確認は別途必要。

## 1.13.1 — ページズーム抑止・ネイティブ操作化

- タイムライン外の`gesturestart/change/end`、複数タッチ、`Ctrl/⌘ + wheel`を抑止する。
- 同じ入力がタイムライン内では既存の時間軸ズームへ渡ることを単体試験する。
- `⌘/Ctrl +`、`-`、`0`によるブラウザ拡大を抑止し、編集ショートカットと入力欄は維持する。
- Chromeの実操作ではページ拡大率が変わらず、タイムライン上だけ拡大率が変わることを確認する。iPhone 16 Pro Safari実機は未検証。

## 1.13.0 — 範囲選択・レイヤー横断移動・等間隔ガイド

- タイムラインの矩形範囲と各素材矩形の交差判定、逆方向ドラッグ、映像／音声をまたぐ選択を自動試験した。
- 選択済み素材からドラッグを開始しても集合を維持するよう、選択と移動開始の順序を修正した。
- 映像・音声・FX・テキストの一括移動は元の時間差を保持し、0秒より前へ移動しない。
- ドラッグ中は選択矩形と色付き等間隔ガイドを表示し、ポインター終了・中止時に確実に消去する。
- ポインター移動ごとの全DOM再探索を廃止し、ジェスチャー開始時に作る要素索引を再利用する。
- Chrome実操作では8項目をレイヤー横断で選択し、全項目が同じ0.233秒だけ移動すること、左右20%の等間隔ガイド表示、実行時エラーなしを確認した。

## 1.12.0 — 書き出し静止化修正・トラック操作・解析カラー

- 端末内書き出しで連続デコード用Canvasが再利用され、同一画像が保持され得る経路を廃止した。出力フレームごとにVFR対応のソース時刻を指定して取得する。時刻が同一のFreeze／Holdだけを再利用するユニット試験を追加した。
- ChromeのWebCodecsで内蔵の動く素材から1秒・30fps、90,533 bytesのH.264/AAC MP4を実生成した。完成ファイルは30フレーム、1.0秒で、0.08／0.48／0.88秒の縮小画面に平均0.46〜0.78階調の差があり、静止画の連続ではないことを確認した。これはiPhone Safariの実機結果ではない。
- タイムライン左側のV1〜V3表示、A1〜A4ミュート／0〜200%音量を追加した。Mac FFmpeg側は上段を非表示にした実MP4で下段の画素へ戻ることを確認した。
- 動画、音声、FX、テキストの複数選択ドラッグを追加し、相対間隔、テキスト尺、Undo単位をユニット試験した。
- UNDERプリセットと、解析結果の平均輝度・10/90パーセンタイル明暗幅・クリップ率を使うCINEMA UNDERを追加した。自動確定は行わず、明示操作時だけ適用する。
- Chrome 1440×960で3映像・4音声の左端操作、50%音量、表示切替、レイアウト、JavaScript例外0件を確認した。
- `.qa-media/real/IMG_7917.mov`の冒頭1秒へUNDER相当の色調整を適用し、540×960・30fpsのH.264/AAC処理プレビューを生成した。0.1秒と0.8秒の平均画素差は36.982で、全尺デコードと元ファイルSHA-256不変を確認した。素材はHEVC/SDR、1080×1920、約58.68fpsで、VFR判定は1.9.0時点の全尺プロキシ検証結果を継続参照する。
- `npm test`、`npm run build`、Pythonのcapabilities/images/layers/multitrack/creative/prores/reliability/server/validate_formats/validate_pipelineを通過した。ローカルFFmpegにない任意機能は事前判定の対象とした。

iPhone 16 Pro Safari、Mac Safari、実写HDR、実写手ぶれ補正、長時間連続編集、主観画質・音質は今回未検証です。

## 1.11.0 — 安定基準・プレビュー・モーション操作

- `v1.10.0`を安定基準としてタグ化し、1.11.0で追加変更を分離した。
- 再生中のシーケンス行をキャッシュし、毎フレームのタイムラインキー生成を省いた。既存の100,000回プレビュー位置検索はNode環境で4.27msだった。これは実機FPSの測定値ではない。
- 動画・静止画のPush in／Pull out／上下左右Panをブラウザプレビュー、端末書き出し、Mac FFmpegへ実装した。パターン静止画の先頭・末尾フレーム差と、H.264/AAC全尺デコードを確認した。
- Option／Altドラッグ中の複製元表示、色付き等間隔バー、FX／テキスト両端トリム、テキストのプレビュー直接移動と中央吸着、文字間隔・行間隔、数値直接入力を追加した。
- Chrome 1440×1000の自動操作で上記操作、動画3個配置、再生を確認し、JavaScript例外は0件だった。
- `npm test`、`npm run build`、Pythonのcapabilities/images/layers/multitrack/creative/prores/reliability/validate_formats/validate_pipelineを通過した。ProResは10bit 4:2:2とPCM、グラデーション256階調超、全尺デコードを確認した。

iPhone 16 Pro Safari、Mac Safari、実写HDR、実写手ぶれ補正、長時間連続編集、主観画質・音質は今回未検証です。

## 1.10.0 — 共通タイムライン操作と直接配置

- ブラウザ版のMEDIAカードを常時ドラッグ可能にし、V1〜V3とA1〜A4への任意位置配置を実装した。読み込み時の自動配置は行わない。
- Shift複数選択、映像・音声・FX・テキスト共通のコピー／切り取り／貼り付け／複製／削除、Optionドラッグ複製を実装した。映像に連動する音声は重複コピーしない。
- CUTに不透明度と透明フェードを追加し、プレビュー上の位置ドラッグ、中央吸着ガイド、タイムライン上の等間隔候補表示を実装した。
- タイムライン領域のCtrl/Command付きwheelとSafari gestureイベントを捕捉し、ページ拡大ではなくタイムラインの1〜10倍ズームに割り当てた。
- テキストのフォント名を安全化して保持し、検出候補と任意色入力を表示した。輪郭は旧プロジェクトを含めて0へ正規化し、DOM／Canvas／書き出しラスターのstroke描画を削除した。
- Chrome 1440×1000の自動操作で、16個のフォント候補、色入力、輪郭操作なし、映像・FX・テキストの複数選択、同時コピー／貼り付け、タイムラインズーム、プレビュー位置変更、素材のドラッグ配置と使用中素材削除を確認した。JavaScript例外は0件だった。
- ユニット試験に中央吸着・サイズ上限、等間隔候補、wheelズーム、輪郭無効化を追加した。

Safari実機のピンチ、Optionドラッグ、フォント表示差、iPhone 16 Pro、長時間編集は未検証です。

## 1.9.1 — FXドラッグ配置・磁気吸着

- FXパレットからFXトラックへのドラッグ配置、配置済みFXとテキストの横移動を実装した。
- 再生ヘッド、映像・音声の編集点、FX・テキスト両端を共通の吸着候補とし、表示倍率に依存しない8画素の判定範囲を使用する。Altによる一時解除、30fpsフレーム丸め、末尾制限、テキスト尺の保持をユニット試験で確認した。
- 素材ドロップと映像クリップ移動にも再生ヘッド吸着を追加し、吸着位置を縦ガイドで表示する。
- Chrome 1440×1000でCamera FlashをFXパレットから5.00秒の再生ヘッドへドロップし、6.43秒へのドラッグ移動、吸着ガイド、Undoによる5.00秒復帰を確認した。操作中のJavaScript例外はなかった。

Mac SafariのHTML5ドラッグ、トラックパッドでの実操作、iPhoneのタッチ移動は未検証です。

## 1.9.0 — エンジン強化・配置動線・連動音声

- `npm test`：PASS。既存試験に加え、分離音声の既定連動、解除・再連動、映像との同時分割、削除、保存復元を検証した。
- `npm run build`：PASS。静的参照、全JavaScript構文、ワークフロー構造、重複IDを検証した。
- Python動画処理テスト：PASS。H.264/AAC、VFR、120fps、240fps入力、速度0.05〜20倍、速度ランプ、4音声トラック、静止画、3映像レイヤー、テキスト、カラー、キャンセル、生成物再検証を通過した。
- ProRes実生成：ProRes 422 / `yuv422p10le` / PCM音声のMOVを生成し、ffprobeと全尺デコードを通過した。通常SDRグラデーションへ小数カラー調整を適用し、高精度内部処理を通した。
- 高品質スロー実生成：FFmpeg `minterpolate` の動き補償設定で0.25倍の出力を生成し、H.264/AAC、尺、全尺デコードを確認した。
- 実写UI通し検証：`IMG_7917.mov`（HEVC/SDR/VFR）とPNGをChrome上のMacエンジンへ読み込み、読み込み直後0クリップ、MEDIAの縦スクロール、ドラッグ配置、右クリック分割、V1〜V3、連動音声、速度ランプ、高品質スロー、Crop、カラー、透明度、映像フェード、Flash、Black Fade、日本語テキスト2層、540p処理プレビューを確認した。6.004秒の処理プレビューは48.3秒で生成し、短い再生3回のJavaScriptヒープ範囲は約1.48MBだった。この短時間測定だけでメモリリークがないとは判断しない。
- ローカルHTTPサーバー試験：認証、Origin制限、Range配信、プロジェクト往復、アップロード、非同期プロキシ、診断ログを確認した。

現在のローカルFFmpegには`vidstab`が含まれていないため、新しい高精度手ぶれ補正設定の実写出力は未検証です。`ffmpeg-full`等の`vidstab`対応ビルドで別途確認が必要です。iPhone 16 Pro Safari、Mac Safari、実写HDR、長時間連続編集も未検証です。

## 1.8.0 — テキスト描画・高度編集・プレビュー応答

- TEXTを最大20レイヤーへ拡張し、文字ごとの選択・複製・削除、タイムライン表示、複数行、日本語、書体、太さ、色、背景、輪郭、整列、位置、不透明度、開始・終了、Fade in/out、Rise／Slideを実装した。
- ブラウザ／iPhone書き出しは共通Canvas描画を使う。Mac版はブラウザが生成した透過PNGをFFmpegのoverlayで合成し、drawtextのないローカルFFmpegでも日本語を出力できることを確認した。PNGは8MB上限・署名・Base64を検査し、.projectと自動保存には保持しない。
- プレビューは文字DOMを再利用し、FX内容が変わらないフレームのDOM更新、停止中の同一動画フレームと静止画のWebGL texture uploadを省いた。80回のフレーム移動後も同じ文字DOMが保持された。
- テキスト正規化・永続データからの一時PNG除外、Canvas共通描画、MacのPNG合成、Mac機能判定を自動試験へ追加した。

高度編集シナリオはApple M2 Max MacBook Pro（12コア、32GB）、macOS 26.5、Chrome 152で実施した。`IMG_7917.mov`（23.177秒、1080×1920、HEVC/SDR、約58.68fps VFR、AAC）と1200×1600 PNGを画面から読み込み、指定位置の右クリック分割、V1〜V3、音声分離、0.65→1.8倍Ease In/Out、Crop、カラー、透明度、映像フェード、Flash、Black Fade、2つの日本語テキストを同時に設定した。

結果：6秒シーケンスの540×960・約30fps処理プレビューを41.775秒で生成した。出力は6.004秒、1,610,724 bytes、H.264/AACで、全尺デコードを通過した。抽出フレームでも2つの日本語文字、背景、色、映像レイヤー合成を目視確認した。3回×約0.9秒の短い再生でJavaScriptヒープは5.78→6.93MB（測定範囲差1.15MB）だった。短時間かつChromeの概算値であり、長時間リーク評価ではない。画面操作中にJavaScript例外はなく、保存済みプロジェクト再読込時の欠落リソースもなかった。

今回確認していないもの：iPhone 16 Pro Safari、SafariのHEVC/HDR/VFRデコード、実写HDR、4K出力、60/120/240fps実写の高度編集、数十分の連続再生・シーク、音声の主観品質、実写手ぶれ補正のCrop量と破綻。これらを検証済みとは扱わない。

## 1.7.3 — ワークフローUI

- 上部ナビゲーションを「素材準備・編集・仕上げ・補助」の4工程に分け、各機能の既存IDと切り替え処理を維持した。
- タイムライン操作を「履歴・クリップ・範囲・編集」の4群に再配置した。デスクトップでは全操作を表示し、800px以下では従来どおりコピー等を「その他」に収める。
- 暗色、余白、境界、選択状態、スクロールバーを統一し、`prefers-reduced-motion`にも対応した。
- `.qa-media/real/IMG_7917.mov` は23.177秒、縦1080×1920、HEVC/SDR、約58.68fpsのVFR、AAC音声として認識した。全尺の720p/30fps H.264プロキシ、サムネイル8枚、波形を生成し、冒頭3秒を1080×1920 H.264/AACへ書き出して全尺デコード検証を通過した。追加のApple Spatial Audioストリームは今回の書き出し対象に含めていない。

Chromeの1440×1000と402×874相当で、レイアウト、工程切り替え、主要ボタンの表示、ビューアーとタイムラインの非重複、JavaScriptエラーがないことを確認した。iPhone 16 Pro SafariとM2 MacBook Proでの実機表示は未検証。

## 1.7.2 — 指定時刻の分割・右クリック

- クリップ内のクリック位置をタイムライン時刻へ変換し、再生ヘッド位置で映像または音声を分割する。端点、GAP、Freezeは分割不可としてメニュー上でも無効にする。
- Mac向け右クリックメニューに、分割、設定表示、コピー、切り取り、複製、属性ペースト、通常削除、前詰め削除を追加した。画面端での位置補正と分割可否をユニット試験で確認した。
- `dist/` はGitHub Pagesの静的公開ディレクトリとして維持し、素材・編集データ・書き出しは同梱しない。`.qa-media/` はGit管理対象外の実写検証専用とする。

1280×820のChrome自動操作で、右クリック位置3.00秒の映像を0–3秒／3–12秒へ、6.00秒の分離音声を0–6秒／6–12秒へ分割した。右端付近のメニューが画面内に収まり、JavaScriptエラーがないことも確認した。

未検証：macOS Safari実機の右クリック／Controlクリック、Magic Mouse・トラックパッド、`IMG_7917.mov` をブラウザ画面から操作する実写分割と端末書き出し。

## 1.7.1 — モバイル作業面・保存管理・機能判定

- 402×874相当で確認された、ビューアーがタイムラインのツールバーとトラックを覆うCSS競合を修正。旧モバイル配置を削除し、ビューアー・タイムライン・素材または設定の3段構成へ統一した。
- タイムラインのピンチ拡大は、距離から1〜10倍へ制限する計算と、ピンチ中央の時刻を維持するスクロール位置をユニット試験に追加した。Safari実機の2本指操作は未検証。
- 端末診断の出力を表示後に削除し、通常書き出しは新しい成功ファイルを記録できた場合だけ前回ファイルを削除する。保存容量画面から保持素材、書き出し数、概算容量を確認できる。IndexedDB／OPFSの実機長時間試験は未実施。
- Mac側はlibx264、vidstab、zscale/tonemap、drawtextを個別表示し、選択編集に必要な機能がない場合はFFmpeg処理開始前に停止する。`tests/capabilities.test.py`で各不足条件を検証した。
- 書き出しプリセットはプロジェクト本体のアスペクト比を変更せず、処理用スナップショットにだけ適用する。編集比率を使う選択肢と、明示した比率の選択肢も追加した。
- GitHub ActionsへPython側の画像、3映像、4音声、耐障害、ローカルサーバー、形式、処理パイプライン試験を追加した。実行環境のFFmpegに任意フィルターがない場合は、その機能を実行済みとはせず、事前停止を検証する。

短い内蔵合成素材による端末書き出しはCodex内ブラウザで成功済み。iPhone 16 Pro Safari、実写HEVC／HDR／VFR、M2 MacBook Proの4K・60/120/240fps、長時間メモリ、画質・音質・速度、実写手ぶれ補正比較は引き続き未検証。

検証環境：Linux x86_64、FFmpeg 6.1系、Python 3.12、Node.js。合成映像に加え、ユーザー提供のH.264/SDR実写素材を使用しました。クラウドChromeで一部UI操作も確認しました。Apple Siliconでの速度測定・実写HDRの色評価・SNS実投稿は未実施です。

## 処理パイプライン

| 試験 | 結果 |
|---|---|
| 4K・60fps・10bit HEVC/HLG生成 → SDRプロキシ | PASS |
| HLG HEVC 4K → 1080p60 H.264 Rec.709 | PASS |
| VFRの検出 → CFR出力 | PASS |
| 120fps音声なし → H.264/AAC | PASS |
| 240fps入力 → 60fps出力 | PASS |
| Trim / 9:16 Crop / 音量・Fade | PASS |
| 0.05倍・20倍 | PASS |
| Ease In/Out 0.1→8倍 + 原音同期 | PASS |
| Frame blending + color + text + hold | PASS |
| vidstab検出・補正2パス | PASS |
| 複数クリップ + 黒の空白 + BGM | PASS |
| Best Moment・Intent別の特徴量解析 | PASS |
| キャンセル | PASS |
| 元素材ハッシュ不変 | PASS |

出力はffprobeでコーデック・映像寸法・映像と音声の長さを確認しています。AACパケットとフレーム境界の丸めによる差を認め、映像/音声の長さ差と期待尺の差は150ms以内を合格条件にしました。ケース別の実測値は `validation/pipeline.json` を参照してください。

解析の全カテゴリはアルゴリズムを実装していますが、実写の検出品質を評価した結果ではありません。表情・ポーズ・視線の意味認識や専門家による候補の採点は含みません。

## 状態・API

- JavaScriptの構文検証：PASS。
- 時間マッピングの相互変換、0.05〜20倍、4種のランプ、空白、プロジェクトJSON：PASS。
- ローカルAPI：未認証拒否、別Origin拒否、メディアのRange配信、ディスク保存/復元、ストリーム読み込み、非同期プロキシジョブ：PASS。
- クラウドChrome：合成素材のImport、WebGL画面表示、再生/停止、フレーム送り、分割、0.05倍→1倍Ease In/Out設定、9:16、COLDルック、Undo/Redo、自動保存後の復帰、再リンク、Best Momentスキャン候補表示を確認。
- `.project` のダウンロード完了イベントは取得できず、実機でのファイル保存・再読込は未検証。UIからの開始と自動保存は動作。
- Safari、ブラウザHEVCデコード、音声試聴、滑らかさの定量評価は未実施。

## メモリ

4K60 HEVC/HLG → 720p30 SDRプロキシ相当のFFmpeg処理、入力とフィルタ・エンコーダのスレッド数を2に制限して測定。

| 入力尺 | 子プロセスの最大RSS |
|---|---:|
| 0.6秒 | 377.5 MiB |
| 6秒 | 379.6 MiB |

短尺で、動画の長さに比例したフレーム蓄積が生じないことを確認しました。長時間稼働・全機能・実機でのリークがないことを保証する測定ではありません。詳細は `validation/memory.json`。

## 採用構成と理由

- HTML/CSS/ES modules：依存パッケージなしで配布し、同じ画面をブラウザとMacで使用。
- HTMLVideoElement＋WebGL：低負荷の近似試写。4KはMacでプロキシ化。
- ネイティブFFmpeg：HEVC、HDR、VFR、vidstab、H.264/AACを同じパイプラインで処理。
- Web Worker：ブラウザのフレーム特徴量計算。毎フレームの画素バッファは処理後に解放。
- Python標準ライブラリ：ローカルHTTPとジョブ制御。127.0.0.1にのみバインドし、起動トークンで認証。
- CPU libx264：この環境で検証できるエンコーダを採用。VideoToolboxは能力の検出のみ。
- FFmpeg.wasm：大きな素材の複製・メモリ圧迫・スタビライズ構成の制約を避けるため、この版では不採用。
- WebCodecs：コーデック対応がブラウザ実装に依存し、HEVC/HDRを必須とするV1の主エンジンには不採用。

## V1受け入れ前に必要な実素材試験

1. iPhoneのSDR 4K60、Dolby Vision/HLG、1080p120/240、縦動画、長尺VFRを各1本。
2. ShingoさんのApple Silicon Macで起動し、ブラウザの動画再生とプロキシ生成を確認。
3. 肌・白い衣服・照明ハイライトを含む映像で、HDR→SDRとカラーの色を評価。
4. 激しいパン・歩き撮り・髪や服の動きで、スタビライズと候補検出を評価。
5. ランプ境界の音、数十分の連続編集、キャンセルと再試行、終了後の再開を確認。
6. Instagramへ実投稿し、画角・再生・色・音声を確認。

未検証項目を合格扱いにはしていません。詳しい操作上の制約は README.md を参照してください。

## 1.1.0 信頼性改善

- MP4/WAVは一時ファイルへ生成し、映像/音声の有無・長さ・コンテナの検証後に確定。
- 不完全な生成物に限り1回再生成。元ファイルや設定のエラーは再試行しない。
- 最終出力はFFmpegで全尺のデコード検証を実施。
- 診断ログを保存し、認証済みAPIからのみ取得可能。
- ブラウザの素材/BGM読み込みと解析シークにタイムアウト・キャンセル・後処理を追加。
- 短いタイムラインは小数秒の目盛りで表示。再リンク待ちに以前の映像を残さない。

`tests/reliability.test.py` の6試験（1回復旧、上限2回と完成済みファイル保護、入力エラー非再試行、キャンセル、音声先頭ストリームの全尺検証、破損プロキシ再生成）全件PASS。`tests/media-state.test.mjs` は成功・即時イベント・キャンセル・タイムアウト・読み込みエラーを検証。更新パイプラインと認証付き診断APIもPASS。

## 実写素材

入力：512×910、H.264 High、8bit SDR Rec.709、30fps CFR、AAC、18.633秒。4K/HEVC/HDR素材ではありません。

- 全尺の1080×1920 H.264/AAC出力、0.05倍・20倍・ランプ混在出力を検証。アップスケールは元映像の細部を増やしません。
- 4〜10秒区間をNATURAL補正、1080×1920で書き出し。1.1.0で2回成功、全尺デコード検証済み。33.16秒/41.65秒（Linuxでの参考値、Mac性能を表しません）。
- 初回の旧処理で中間MP4の `moov atom not found` を1回観測。原因は未特定。同条件の旧処理再試行2回と更新版2回は成功。自動再生成が元の原因を解消したとは結論しません。
- 元素材のハッシュ不変を確認。人物・髪の動きにBest/Motion候補あり。この素材では静止/低変化区間の候補なし。人物意味認識の品質試験ではありません。
- `validation/reliability-real.json` は集計のみ。実写動画本体は同梱していません。

## 1.2.0 iPhone / 端末内書き出し

追加経路：Mediabunny 1.56.0 + WebCodecs + Worker + OffscreenCanvas。H.264/AACを時刻指定で生成し、OPFSへ位置指定の小さな書き込みを行います。Fast Startはメタデータ領域の事前予約方式。全フレームと音声をメモリへ蓄積しません。処理後に映像・音声を全尺デコードし、フレーム数・コーデック・尺を検証します。

端末内に素材のBlobをIndexedDBで保存し、自動保存されたプロジェクトから再開します。元の写真ライブラリは変更しません。完成MP4は共有画面またはdownloadリンクで保存し、直近の完成出力をOPFSに保持します。共有操作が返ったことを写真への保存完了とは判定しません。

### 実施した試験

- 出力寸法（1080p/4K、9:16）、Source FPS上限60：PASS。
- 全4ランプ、0.05〜20倍、Holdを含む映像時刻計算：既存モデルとの一致PASS。
- ステレオ補間、音量、Fade、補正用の既知の平行移動推定とcrop上限：PASS。
- 本番と同じMediabunny MP4 muxer / StreamTargetでH.264/AACの実パケットを位置指定書き込み。先頭音声ストリームの合成入力、Fast Start、ffprobe尺検証とFFmpeg全尺デコード：PASS。
- 実PCM音声をストリームで読み、サンプル境界以外からの区間取得、速度ランプ混合、Hold/Mute、BGMループ境界を検証：PASS。
- JavaScript構文・静的アセット参照、従来の時間モデル・メディアイベント試験：PASS。
- アプリの「端末版の対応状況」に短尺の書き出しテストを実装。ユーザー端末でWorker・デコード・GPU・エンコード・OPFS・出力検証を実際に通せます。テストは編集プロジェクトを変更しません。

### 未検証・非同等の項目

この実装はiPhone実機で未検証です。上記のMP4試験はmuxer/IOの検証であり、iOSのWebCodecsエンコーダ・GPU処理を通した試験ではありません。iPhone Safariでのタッチ操作・写真共有保存・ファイル保存・長尺/4Kメモリ測定は未実施です。「完全対応済み」とはしません。

- HDRは検出して停止します。ブラウザの不確かな暗黙変換で色を確定しないためです。現状、正確なHLG/PQ→SDRはMac版が必要です。
- 端末内の原音速度変更はリサンプリングで、音程も変化します。BGMは通常速度。Mac版のピッチ維持とは異なります。
- 端末内スタビライズは低解像度での平行移動追跡と平滑化です。回転・パース・ローリングシャッターやvidstab同等の補正は行いません。品質は実写で未評価です。
- Frame blendingは隣接フレームの時刻補間です。光学フローではありません。
- 画面が背景に移ると書き出しを中止し、編集を保持します。バックグラウンド書き出しは保証しません。
- 端末版はWebCodecsのH.264/AAC、Worker内OffscreenCanvas/WebGL、OPFS同期アクセスに依存します。バージョン番号だけで対応済みとはせず、実際の能力を判定します。
- ブラウザ内の保存は、OSの空き容量不足やサイトデータの削除で失われる場合があります。写真/ファイルへ完成品を保存し、元素材は保持する必要があります。

参照： https://mediabunny.dev/guide/writing-media-files 、 https://mediabunny.dev/guide/media-sinks 、 https://webkit.org/blog/13862/the-user-activation-api/ 。バンドルにMITライセンスを同梱。

## 1.3.0 — 2レイヤー / 直接編集 / プレビュー負荷軽減

2026-09-09、Linux環境で確認。

- 2レイヤーの可視区間、上段優先、下段への復帰、空白、コピーの独立性、削除、同一レイヤーの前詰め、旧データの配置変換、JSON再読込を確認。
- Pointerイベントのユニットテストで左右トリム、1操作1チェックポイント、上下レイヤー移動、キャンセル時のハンドラ解除を確認。実ブラウザ／実機タッチ操作の検証ではない。
- Linear / Ease In / Ease Out / Ease In-Outのランプ分割で、元の時間積分とソース位置が保存されることを確認。
- ネイティブFFmpegで赤→青→赤の2レイヤーMP4を実生成。フレーム境界外の編集点、H.264/AAC、全尺デコード、RGBサンプル、原本SHA不変を確認。
- 端末版音声処理で、ミュートされた上段が下段原音を覆い、下段へ戻ると正しい時間の原音が復帰することを確認。端末エンコーダそのものは未検証。
- 既存のプロキシ、0.05x、20x、速度ランプ、カラー、テキスト、保持、手ぶれ補正、複数クリップとBGM、解析、120fpsのパイプライン試験が通過。
- 再生開始時に可視タイムラインをコンパイル。200クリップのテストで時刻検索100,000回が約5〜6ms（Nodeの参考値。iPhoneの再生FPSや動画デコード速度ではない）。
- 速度積分・GPU uniformをキャッシュ。再生中の描画は最大30回/秒、ドラッグ中は既存クリップDOMの位置と幅を更新。

未検証：iPhone/Safari実機、実機共有保存、長時間4Kの速度とメモリ、実ブラウザのジェスチャー。端末HDR非対応など1.2.0の制限は継続。今回、透過合成と2本の原音の同時ミックスは追加していない。

## 1.4.0 — 静止画・音声・素材管理

2026-09-09、Linux環境。

- 資産モデル試験：拡張子／MIMEによる種類判定、検索、種類フィルター、元ファイル名を保つ表示名変更、使用中素材の保護、未使用の除外、JSON復元を確認。
- 静止画の初期5秒、12秒への延長、画像プレビューの古い非同期デコードを破棄・解放する処理を確認。
- 実FFmpeg試験：PNG静止画を6秒へ延長＋H.264動画0.5秒＋WAVのBGMループをH.264/AAC MP4として書き出し。RGBサンプル、6.5秒の長さ、全尺デコード、元素材SHA不変を確認。
- 既存JSテスト一式（レイヤー・ジェスチャー・速度・端末コンテナ・端末音声）およびローカルサーバーの認証・アップロード・プロキシ・保存試験が通過。
- 音声付きファイルに埋め込まれたアルバム画像を、映像トラックと誤認しないようネイティブ解析を修正。
- 実ブラウザQA、iPhone実機、端末エンコーダでの静止画書き出しは未検証。画像デコードの破棄はモックを用いたユニット試験であり、実機性能を表すものではない。


## 1.5.0 — Creative tools

- tests/creative.test.mjs：静止の元時刻不変、尺キャッシュ更新、ランプと分割済みランプの伸縮、速度上限下限、マウス／2本指の1トランザクションとキャンセル、透明度／FX／文字の時刻を検証。
- 8ルック×4強度×3色（肌色・明部・暗部）の96組でJavaScriptとPythonのRGB計算が一致。MONOの強度150%でも無彩色を維持。
- tests/creative.test.py：赤V1／青V2を実際にH.264/AAC化。開始は赤、中間は両色、中央は青になる透明フェードを画素で検証。白フラッシュ・黒から映像・映像から黒も画素を検証。
- 動く合成素材の末尾0.999秒を2秒静止。元フレームをffprobeの実タイムスタンプから選び、離れた時刻のフレームが一致。静止＋Rise文字のMP4全尺デコードも通過。
- LUT出力の画素テストでRGB→YUV変換行列とBT.709タグの不一致を発見し修正。補正式との差を圧縮／量子化を含む許容範囲内で検証。
- 既存のJSモデル／音声／コンテナ試験、ネイティブの速度極値・ランプ・カラー・保持・文字・スタビライズ・BGM・解析・120fpsパイプラインを検証。
- ブラウザ／iPhone16 Pro／M2 MacBook Proで今回のUI・GPU・タッチ・実時間FPSは未検証。端末内合成コードの実エンコードは端末テストで確認が必要。LUT補間、コーデック、フォント、HDR/SDR色管理で完全な画素一致は保証しない。


## 1.6.0 — Three video / four audio lanes

- tests/multitrack.test.mjs：V3優先、動画原音の非破壊分離、独立移動／分割、4段とsolo/mute、音声末尾による総尺延長、分離音声の素材保持、JSON復元、旧BGMの移行を検証。
- tests/mobile-multitrack.test.mjs：実PCM音声を4本デコードし、端末ミキサーで同時合成。ループ境界を跨ぐ0.25秒窓の4周波数と、solo/mute後の周波数成分を検証。
- tests/multitrack.test.py：透過するV3/V2/V1をH.264/AACへ実際に出力し、合成画素と4音声の周波数成分を検証。soloと音声が映像より0.7秒長い黒の末尾を含む出力も全尺デコードに通過。
- 既存creative native testで透明フェード・フラッシュ・黒フェード・静止末尾・文字・色出力の回帰を検証。
- ソース静的検査で単一要素セレクタへのforEachを検出し修正。画面機能のブラウザ実行／Safari実機／実時間FPS・長尺メモリ測定は今回未実施。レイアウトは画面内パネルと各パネルのスクロール、拡大切替でアクセスを確保する構造へ変更。
